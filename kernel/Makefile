# adjust number of cores in qemu.mk
NAME           := kernel
SRCS           := framework/simple_test/alpha.c $(shell find -L ./src/ -name "*.c")
INC_PATH       := include/ framework/

include $(AM_HOME)/Makefile
include ../oslabs.mk
