// DO NOT MODIFY: Will be reverted by the Online Judge.

#include <common.h>

int main(const char *args) {
    ioe_init();
    cte_init(os->trap);
    os->init();
//    init_workload(args);
    mpe_init(os->run);
}
